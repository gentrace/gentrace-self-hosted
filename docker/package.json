{
  "name": "gentrace-self-hosted-docker-test",
  "version": "1.0.0",
  "description": "Minimal seed and test scripts for self-hosted Gentrace docker-compose testing",
  "private": true,
  "scripts": {
    "seed": "tsx seed-minimal.ts",
    "test-seed": "tsx test-seed-data.ts",
    "validate": "npm run test-seed"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "prisma-field-encryption": "^1.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}

